## VECTOR 

```
#include<vector>

// declaration : 
vector<int> v;
vector<int> v(30);

// initialization (giving values at the time of creating it) :
vector<int> v({1,2,3,4,5});

// sorting a vector 
sort(v.begin(),v.end());